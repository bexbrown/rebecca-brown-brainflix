import "./Loading.scss";

function Loading() {
    return (
        <section className="loading">
            <h1 className="loading__heading">Loading...</h1>
        </section>
    )
}

export default Loading;